import { NumberValueAccessor } from "@angular/forms";

export interface Phone {
    image: string;
    name: string;
    brand: string;
    price: number;
    stock: number;
    quantity: number;
    
}